#include "main.h"
/**
* main - Prints the alphabet in lowercase
*/
extern void print_alphabet(void);
{
	char letter;

	while (letter = 'a'; letter <= 'z'; letter++)
		_putchar(letter);

	_putchar('\n');
return 0;
}
